package com.quocnva.easymall.config;

import org.springframework.context.annotation.Configuration;

/**
 * CORS configuration — allowed origins, methods, and headers for all endpoints.
 */
@Configuration
public class CorsConfig {
}
